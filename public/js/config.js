var primary = localStorage.getItem("primary") || '#2315bd';
var secondary = localStorage.getItem("secondary") || '#e3ba32';

window.CubaAdminConfig = {
	// Theme Primary Color
	primary: primary,
	// theme secondary color
	secondary: secondary,
};
